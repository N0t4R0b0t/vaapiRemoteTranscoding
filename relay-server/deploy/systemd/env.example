# Copy to /etc/relay-server/env (referenced by both unit files).

RELAY_LISTEN=0.0.0.0:9100
RELAY_RESOLUTION=640x480
RELAY_QUALITY=4

# Only read by `relay-server pull` (Phase 1 test mode) -- a real source
# ffmpeg can open, e.g. a test file path or an rtsp:// URL.
RELAY_SOURCE=
